/** @type {import('next').NextConfig} */
const nextConfig = {}

// eslint-disable-next-line no-undef
module.exports = nextConfig

// eslint-disable-next-line no-undef
module.exports = {
    reactStrictMode: false,
    images: {
      domains: ['images.costco-static.com'],
    },
  }
